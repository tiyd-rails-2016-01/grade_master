<tr>
  <td><%= student.first_name %></td>
  <td><%= student.last_name %></td>
  <td><%= "#{student.teacher.first_name} #{student.teacher.last_name}" if student.teacher %></td>
  <td><%= link_to 'Show', student %></td>
  <td><%= link_to 'Edit', edit_student_path(student), remote: true %></td>
  <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' }, remote: true  %></td>
  <td><%= link_to 'Manage assignments', student_grades_path(id: student.id), remote: true %></td>
  <td><%= link_to "Manage achievements", student_achievements_path(id: student.id), remote: true %></td>
</tr>
